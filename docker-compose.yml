version: '3.8'

services:
  jupyter:
    build: .
    ports:
      - "8888:8888"
    volumes:
      - ./notebooks:/workspace/notebooks
    environment:
      - JUPYTER_ENABLE_LAB=yes
    command: julia --project=. -e 'using IJulia; IJulia.notebook(dir="/workspace/notebooks", detached=false)'
